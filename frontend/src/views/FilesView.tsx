import { Card } from '../components/ui/Card';

export function FilesView() {
  return (
    <div className="p-3 h-full">
      <Card className="min-h-[300px] flex items-center justify-center text-[var(--color-text-muted)]">
        Google Drive / Photos browser stub coming soon.
      </Card>
    </div>
  );
}
